#!/bin/bash
echo "source python.sh"

# python env settings
  export FIAT_req_path="${DIR}/requirements/"
  export FIAT_req_file="${environment_type}.txt"
  export FIAT_requirements=${req_path}${req_file}